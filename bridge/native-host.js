/**
 * DEPRECATED — File ini tidak lagi digunakan.
 *
 * Arsitektur v3 menggunakan WebSocket (bukan Native Messaging)
 * sebagai transport antara Extension dan Bridge Server.
 * Ini agar kompatibel dengan skenario WSL (agent di Linux) + Browser di Windows.
 *
 * Lihat bridge/index.js untuk implementasi yang aktif.
 * File ini disimpan sebagai referensi jika di masa depan
 * ingin menambahkan mode Native Messaging opsional.
 */
